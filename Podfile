# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'myTips' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Firebase/Crash'

  # Pods for myTips
		
  target 'myTipsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'myTipsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
